---
interface Props {
  class?: string;
  /** on-light = dark logo for white backgrounds */
  variant?: "on-dark" | "on-light";
}

const {
  class: className = "h-11 w-auto sm:h-12",
  variant = "on-dark",
} = Astro.props;
---

<img
  src="/logo.png"
  alt="Makdesi And Associates Lawyers"
  class:list={[
    "block max-w-none object-contain",
    variant === "on-dark" ? "mix-blend-screen" : "brightness-0",
    className,
  ]}
  width="480"
  height="160"
/>
